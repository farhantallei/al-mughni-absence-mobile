import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  greet: {
    fontSize: 32,
  },
  top: {
    flex: 0.5,
    alignItems: 'center',
    justifyContent: 'flex-end',
    paddingBottom: 64,
  },
  bottom: {
    flex: 1,
  },
});
